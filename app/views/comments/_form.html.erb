<%= form_for([@article, @article.comments.build]) do |f| %>
  <p>
  	Commenter Name:
    <!--TODO: fix this to point at user name-->
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>